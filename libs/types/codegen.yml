overwrite: true
schema: '../../apps/server/src/schema/schema.graphql'
documents: []
generates:
  src/generated/graphql.ts:
    config:
      scalars:
        Date: 'Date'
    plugins:
      - 'typescript'
